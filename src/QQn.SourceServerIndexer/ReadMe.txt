The QQn.SourceServerSharp assembly contains the resolver code and
the MSBuild task. It requires only the .Net 2.0 framework to compile.

At runtime the 'Debugging Tools For Windows' SDK-component is 
required (the SRCTOOL.EXE and PDBSTR.EXE are used for annotating).